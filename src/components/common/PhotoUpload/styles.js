import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  avatar: {
    width: 100,
    height: 100,
    borderColor: '#3f51b5',
    borderWidth: 4,
    borderRadius: 50,
    justifyContent: 'center',
    alignItems: 'center'
  }
})