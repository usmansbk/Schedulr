import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    height: 48,
    width: 48,
    justifyContent: 'center',
    alignItems: 'center',
  },
  icon: {
    color: '#ffffff',
    fontSize: 24
  }
})