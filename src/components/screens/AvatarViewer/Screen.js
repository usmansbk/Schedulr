import React from 'react';
import Viewer from 'component/common/ImageViewer';

export default class AvatarViewer extends React.Component {
  render() {
    return (
      <Viewer
      />
    );
  }
}